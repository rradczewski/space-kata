var should = require('chai').should();

describe('the line wrapping algorithm', function() {
	xit('should return a one-element array for a single line', function() {
	});

	xit('should split a text with length exceeding the line width into parts', function() {
	});

	xit('should not break a word into chunks. Instead it should move it to the next line', function() {
	});

	xit('should break a word with a dashinto two parts with the dash staying intact', function() {
	});
});


